[color]
    ui = auto
[alias]
    a = add
    cl = clone
    f = fetch
    l = log
    s = status -sb
    co = checkout
    b = branch
    ci = commit
    d = diff
    ds = diff --staged
    g = grep
    gr = log --graph --date-order --all\
         --pretty=format:'%h %Cred%d %Cgreen%ad %Cblue%cn %Creset%s' --date=short
    p = push
    r = rebase
    m = merge
[github]
    user = furu
[push]
    default = simple
[include]
    path = .gitconfig.local
[merge]
    ff = false
